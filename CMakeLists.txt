# this is the build file for project 
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.13.0)
project(managedc-test LANGUAGES C CXX ASM)

# target
add_executable(managedc-test "")
set_target_properties(managedc-test PROPERTIES OUTPUT_NAME "managedc-test")
set_target_properties(managedc-test PROPERTIES RUNTIME_OUTPUT_DIRECTORY "build/linux/x86_64/release")
target_include_directories(managedc-test PRIVATE
    src
)
target_compile_options(managedc-test PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-m64>
    $<$<COMPILE_LANGUAGE:CXX>:-m64>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
)
if(MSVC)
    target_compile_options(managedc-test PRIVATE $<$<CONFIG:Release>:-Ox -fp:fast>)
else()
    target_compile_options(managedc-test PRIVATE -O3)
endif()
target_link_options(managedc-test PRIVATE
    -m64
)
target_sources(managedc-test PRIVATE
    src/test.c
    src/managed.h
)

